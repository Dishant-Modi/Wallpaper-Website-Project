; Enables output buffering so session_start()/header() calls succeed even if
; HTML has already been echoed earlier in a script -- matches the default
; behavior of most shared PHP hosts (e.g. InfinityFree), which the stock
; php:8.2-apache image doesn't have on by default.
output_buffering = 4096
display_errors = Off
